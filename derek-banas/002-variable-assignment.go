package main

func fn002() {
    // Variable assign
    var myName string = "John Doe"
    var x, y, z = 1.0, 2.0, 3.0 // Multiple assignments at once
    foo := "bar" // Without the var use :=
    _, _, _, _, _ = myName, x, y, z, foo
}
